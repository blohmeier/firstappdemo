package mainpackage;

public class convert {
	public static void main(String[] args)
	{
		int age=40;
		double salary=50000.09;
		boolean isTrue=true;
		String rate="3.5"
		
		// convert to string
		String NewSalary=String.valueOf(salary); //double to string
		String NewAge=String.valueOf(age); //int to string
		String NewisTrue=String.valueOf(isTrue); //boolean to string
		
		// convert to int
		int new_rate=Integer.parseInt(rate);
		int new_salary=
	}

}
